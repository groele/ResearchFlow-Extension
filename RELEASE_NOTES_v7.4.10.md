# ResearchFlow Companion v7.4.10

- Improved the + Event workflow with clear field semantics, keyboard Enter submission, and visible save feedback.
- Added busy-state protection to prevent duplicate event creation while persistence is in progress.
- Added safe error recovery so storage failures leave the editor usable instead of breaking the dashboard.

